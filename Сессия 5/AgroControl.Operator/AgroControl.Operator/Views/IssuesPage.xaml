<UserControl x:Class="AgroControl.Operator.Views.IssuesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F9FAFB">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Обновить" Click="Refresh_Click" Width="100" Style="{StaticResource PrimaryButton}"/>
        </StackPanel>

        <WrapPanel Grid.Row="1" Margin="0,0,0,8">
            <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <TextBox x:Name="txtSearch" Width="200" Margin="0,0,10,0"/>
            <Button Content="Искать" Click="Search_Click" Width="80" Style="{StaticResource PrimaryButton}"/>
            <Button Content="Сброс" Click="ClearSearch_Click" Width="80" Style="{StaticResource PrimaryButton}" Margin="6,0,0,0"/>
        </WrapPanel>

        <DataGrid x:Name="dgIssues" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True"
                  AlternatingRowBackground="#F9FAFB" CanUserResizeColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID партии" Binding="{Binding ПартияПроизводстваID}" Width="80" MinWidth="60"/>
                <DataGridTextColumn Header="Время" Binding="{Binding ВремяСобытия, StringFormat=dd.MM.yyyy HH:mm:ss}" Width="150" MinWidth="120"/>
                <DataGridTextColumn Header="Тип" Binding="{Binding ТипСобытия}" Width="120" MinWidth="100"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Описание}" Width="*" MinWidth="200">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Важность" Binding="{Binding Важность}" Width="100" MinWidth="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{Binding Важность, Converter={StaticResource StatusToColorConverter}}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Создал" Binding="{Binding Создал.ПолноеИмя}" Width="150" MinWidth="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>